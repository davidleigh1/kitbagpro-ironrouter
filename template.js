if (Meteor.isClient) {

// Template.myTemplateName.helpers


// Template.myTemplateName.events


};

if (Meteor.isServer) {
	// None currently defined
};

Meteor.methods({
	// None currently defined
});